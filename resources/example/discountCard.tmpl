<table class="discount" align="center" width="100%" cellpadding="0" cellspacing="0" role="presentation">
    <tr>
        <td align="center">
            <h1 class="f-fallback discount_heading">{{$head}}</h1>
            <p class="f-fallback discount_body">{{$body}}</p>
            <table width="100%" border="0" cellspacing="0" cellpadding="0" role="presentation">
                <tr>
                    <td align="center">
                        <a href="{{$link}}" class="f-fallback button button--green" target="_blank">{{$text}}</a>
                    </td>
                </tr>
            </table>
        </td>
    </tr>
</table>
